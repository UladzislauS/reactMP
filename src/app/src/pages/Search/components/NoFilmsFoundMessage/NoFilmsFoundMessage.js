import React from 'react';
import ErrorMessage from '../../../../common/styles/ErrorMessage.styles';

const NoFilmsFoundMessage = () =>
    <ErrorMessage>No films found</ErrorMessage>;

export default NoFilmsFoundMessage;
